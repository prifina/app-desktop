import React from "react";
import DeclineDialog from "../src/components/DeclineDialog";

export default { title: "DeclineDialog" };

export const dialog = () => <DeclineDialog />;
dialog.story = {
  name: "DeclineDialog",
};
